import { MthTemperatureDecimalPipe } from './mth-temperature-decimal.pipe';

describe('MthTemperatureDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new MthTemperatureDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
