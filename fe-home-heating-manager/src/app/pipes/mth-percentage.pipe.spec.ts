import { MthPercentagePipe } from './mth-percentage.pipe';

describe('MthPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new MthPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
