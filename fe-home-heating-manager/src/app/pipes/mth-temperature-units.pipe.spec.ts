import { MthTemperatureUnitsPipe } from './mth-temperature-units.pipe';

describe('MthTemperatureUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new MthTemperatureUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
