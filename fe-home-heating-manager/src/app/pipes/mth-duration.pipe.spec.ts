import { MthDurationPipe } from './mth-duration.pipe';

describe('MthDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new MthDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
