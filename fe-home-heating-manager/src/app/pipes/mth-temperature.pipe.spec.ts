import { MthTemperaturePipe } from './mth-temperature.pipe';

describe('MthTemperaturePipe', () => {
  it('create an instance', () => {
    const pipe = new MthTemperaturePipe();
    expect(pipe).toBeTruthy();
  });
});
