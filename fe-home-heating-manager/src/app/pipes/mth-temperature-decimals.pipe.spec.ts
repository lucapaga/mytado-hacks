import { MthTemperatureDecimalsPipe } from './mth-temperature-decimals.pipe';

describe('MthTemperatureDecimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new MthTemperatureDecimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
