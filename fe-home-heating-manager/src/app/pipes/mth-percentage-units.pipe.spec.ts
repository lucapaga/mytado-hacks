import { MthPercentageUnitsPipe } from './mth-percentage-units.pipe';

describe('MthPercentageUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new MthPercentageUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
